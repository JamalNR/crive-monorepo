# CRIVE — .env.example (TEMPLATE TANPA RAHASIA)
# Klasifikasi: tandai dengan # REQUIRED / # OPTIONAL
# Cara pakai: salin file ini menjadi `.env` lalu isi nilainya di lokal. Untuk prd/stg gunakan Secrets Manager (Doppler).

# ====== GLOBAL (shared) ======
NODE_ENV=development                         # REQUIRED  (development|staging|production)
LOG_LEVEL=info                               # OPTIONAL  (trace|debug|info|warn|error)
TZ=Asia/Jakarta                              # OPTIONAL
# Catatan: nilai di file ini hanyalah contoh aman; TIDAK BOLEH ada rahasia asli.

# ====== API (apps/api) ======
API_PORT=4000                                # OPTIONAL  default: 4000
DATABASE_URL=postgres://USER:PASS@HOST:5432/DBNAME   # REQUIRED  ISI DI .env lokal; prd/stg via Doppler
API_JWT_SECRET=__SET_IN_LOCAL_ENV__          # REQUIRED  contoh placeholder; JANGAN isi rahasia di file ini
API_CORS_ORIGINS=https://crive.app,https://admin.crive.app   # OPTIONAL  comma-separated

# ====== WEB / ADMIN (apps/admin - Next.js) ======
NEXT_PUBLIC_APP_NAME=Crive                   # OPTIONAL
NEXT_PUBLIC_API_URL=http://localhost:4000    # REQUIRED  (dev); gunakan https://api.crive.app di prd
NEXT_TELEMETRY_DISABLED=1                    # OPTIONAL
