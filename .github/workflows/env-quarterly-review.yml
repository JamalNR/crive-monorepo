name: env-quarterly-review

on:
  workflow_dispatch:
  schedule:
    - cron: '0 8 1 */3 *'   # optional: tiap 3 bulan tgl 1 jam 08:00 UTC

permissions:
  contents: read

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Quarterly ENV review ping ✅"
