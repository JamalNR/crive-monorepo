name: env-quarterly-review
on:
  workflow_dispatch:
jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash scripts/quarterly_review.sh
        shell: bash
